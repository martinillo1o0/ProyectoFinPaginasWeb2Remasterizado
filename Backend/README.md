# Marti Music API

API REST musical construida con NestJS 11, TypeORM 1.x y MariaDB. Incluye autenticación JWT, roles, canciones, géneros, playlists, paginación y seed.

Consulta el `README.md` de la raíz para instrucciones completas.
